# Exceptions to standard SeaORM and Seaography

Only Richard adds entries. Each entry names the exact construct, the file, why the standard
mechanism cannot express it, and the date it was granted. `npm run check:idiomatic` reads this
file; anything not listed here counts as a violation.

See [`idiomatic-seaography-plan.md`](./idiomatic-seaography-plan.md).

## Granted

None.
