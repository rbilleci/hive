# Leptos erases component and view types behind this cfg. Without it a one-line edit to a large view
# recompiles in about half a minute; with it, about two seconds.
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "erase_components"]
